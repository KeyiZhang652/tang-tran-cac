# 藏珍阁 · Tàng Trân Các — 网站使用说明

这是为您岳父打造的收藏展示网站。**双击 `index.html` 即可在浏览器中打开**，无需安装任何软件。

## 一、语言切换

页面右上角有 **VI / 中文 / EN / FR** 四个按钮（越南语、中文、英语、法语）。

- 首次打开时会自动根据访客浏览器的语言显示（默认越南语）；
- 访客手动切换后，下次打开会记住上次的选择。

## 二、如何放入真实照片（最重要）

把照片文件放进 `photos/` 文件夹，并按以下文件名命名，页面会**自动显示**（照片缺失时显示优雅的占位图，不影响页面美观）：

| 文件名 | 显示位置 |
|---|---|
| `photos/portrait.jpg` | "关于藏主"版块的肖像照 |
| `photos/01.jpg` | 珍品掠影 第 1 张（默认说明：ODO 摆钟） |
| `photos/02.jpg` | 第 2 张（Honda 67 摩托车） |
| `photos/03.jpg` | 第 3 张（Peugeot 自行车） |
| `photos/04.jpg` | 第 4 张（瑞士怀表） |
| `photos/05.jpg` | 第 5 张（钵场青釉水缸） |
| `photos/06.jpg` | 第 6 张（红木罗汉床） |

> 建议：照片裁成竖版（宽高比约 4:5）效果最佳；每张控制在 500KB 以内加载更快。

**全屏大图展位（苹果官网式滚动效果，强烈建议上传）**：

| 文件名 | 显示位置 |
|---|---|
| `photos/feature-1.jpg` | 第一章全屏展位（ODO 摆钟） |
| `photos/feature-2.jpg` | 第二章全屏展位（Honda 67） |
| `photos/feature-3.jpg` | 第三章全屏展位（红木家具） |

> 这三张是整个网站视觉冲击力最强的位置：滚动时图片会缓缓放大定格、文字浮现。请选**横版、高清**的照片（宽度 ≥1600px，宽高比约 16:10，每张 1MB 以内），拍摄时物件居中、背景干净最出效果。没上传时会显示雕版线稿占位图，不影响浏览。

**修改照片说明文字**：用文本编辑器打开 `index.html`，搜索 `"g1"`、`"g2"`……即可找到每张照片在四种语言里的说明文字，直接改引号里的内容即可。

## 三、需要替换的占位信息

以下内容目前是占位符，请用文本编辑器打开 `index.html` 搜索替换：

1. **电话 / Zalo**：搜索 `+84 90 000 0000`（共 2 处显示文字）和 `84900000000`（2 处链接），换成真实号码；
2. **邮箱**：搜索 `suutap.thoigian@gmail.com`（2 处），换成真实邮箱；
3. **Facebook**：搜索 `fb.com/tangtrancac` 和 `https://facebook.com`，换成真实主页地址；
4. **数字统计**：首屏的 `300+`（藏品件数）、`30+`（收藏年数）可按实际情况修改；
5. **来访城市**：搜索 `"ct.visitv"`，可把"越南"改成具体城市，如"胡志明市，越南"（注意四种语言各有一处）；
6. **收藏时间线的年份和事件**：目前 1992 / 2001 / 2010 是示意年份，搜索 `"tl1.y"`、`"tl2.y"`、`"tl3.y"` 按真实经历修改（四种语言各一份，年份数字可以只改一处再复制）。

## 四、网站已发布 ✅

网站已通过 GitHub Pages 上线，任何人都可以访问：

- **网址**：<https://keyizhang652.github.io/tang-tran-cac/>
- **代码仓库**：<https://github.com/KeyiZhang652/tang-tran-cac>（公开仓库）

### 以后如何更新网站内容

改完本地文件后，在终端进入 `网页` 文件夹执行：

```bash
git add -A
git commit -m "更新内容"
git push
```

推送后约 1 分钟，网站自动更新。

**不想用命令行？** 也可以直接在浏览器里操作：打开仓库页面 → 进入 `photos` 文件夹 → 点 "Add file → Upload files" 把照片拖进去提交即可；改文字则点开 `index.html` 后按铅笔图标在线编辑。

## 五、背景音乐

- 曲目：**肖邦《G大调夜曲》Op. 37 No. 2**（Olga Gurevich 演奏，来自 Musopen，CC0 公有领域授权，可放心商用/公开使用，无需署名）；
- 文件位置：`music/nocturne.mp3`，想换曲子就用同名文件替换（找音乐时务必确认是 CC0/公有领域授权）；
- 音量默认 22%（轻柔背景）。想调整就在 `index.html` 里搜索 `MUSIC_VOL`，改成 0～1 之间的数；
- 右下角的**喇叭图标**随时可以关闭/打开音乐，访客的选择会被记住（关过一次，下次访问就不会自动响）；
- 注意：所有浏览器都禁止网页自动出声，所以音乐会在访客**第一次点击或触摸页面后**才轻轻响起，这是正常现象，不是故障。

## 六、其他说明

- 网站名"藏珍阁 / Tàng Trân Các"意为"珍藏之阁"，中越通用（越南语为汉越音）。若想改名，搜索 `TÀNG TRÂN CÁC` 和 `藏珍阁` 替换；
- 字体通过 Google Fonts 在线加载，断网时会自动退回系统字体，页面依然可看；
- 六大门类的介绍文字里特意提到了越南收藏圈的经典元素（ODO 摆钟、Honda 67、Peugeot 自行车、钵场/边和水缸、红酸枝家具），懂行的藏友一看就知道是同道中人。
